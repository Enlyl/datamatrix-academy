const module6 = {
    id: 6, title: "Statistics for DS", icon: "📐",
    description: "Статистика без боли: учимся понимать данные через средние числа и связи.",
    lessons: [
        {
            id: "6-1", title: "Центр данных: Где правда?",
            theory: [
                { type: "text", content: "### 1. Аналогия: Зарплаты в компании\nПредставь компанию, где 10 человек получают по 50к, а директор — 1 миллион. \n• **Среднее (Mean)** скажет, что все получают по 136к (ложь!). \n• **Медиана (Median)** скажет 50к (правда!). \n\nВ DS медиана — твой лучший друг, когда в данных есть «выскочки» (выбросы)." },
                { type: "text", content: "### 2. Как считать на Python" },
                {
                    type: "code", language: "python", content: `import numpy as np
salaries = np.array([50, 50, 50, 1000])

print(np.mean(salaries))   # 287.5
print(np.median(salaries)) # 50.0`
                },
                { type: "text", content: "### 3. Практический пример: Детектор аномалий\nЕсли среднее и медиана сильно разошлись, значит, в твоих данных «затесался директор». Профи всегда проверяют это перед обучением модели:" },
                {
                    type: "code", language: "python", content: `diff = abs(df['price'].mean() - df['price'].median())
if diff > df['price'].std():
    print("Внимание: в данных есть сильные выбросы!")`
                }
            ],
            quiz: {
                type: "choice",
                question: "Какая метрика лучше всего описывает 'типичную' зарплату в стране, если есть 1% миллионеров, которые сильно завышают показатели?",
                options: ["Среднее арифметическое", "Медиана", "Сумма", "Дисперсия"],
                correct: 1,
                hint: "Медиана устойчива к выбросам (миллионерам)."
            }
        },
        {
            id: "6-2", title: "Корреляция: Связь или совпадение?",
            theory: [
                { type: "text", content: "### 1. Аналогия: Мороженое и акулы\nЛетом растут продажи мороженого и количество нападений акул. Значит ли это, что мороженое приманивает акул? Нет! У них просто одна причина — жара. \n\n**Корреляция (r)** показывает силу связи от -1 до 1, но не говорит КТО виноват." },
                { type: "text", content: "### 2. Код для поиска связей" },
                {
                    type: "code", language: "python", content: `import pandas as pd
# Связь оценки игры и её продаж
r = df['score'].corr(df['sales'])
# Если r > 0.7 — связь сильная`
                },
                { type: "text", content: "### 3. Практический пример: Отбор признаков\nВ машинном обучении мы удаляем признаки, которые никак не коррелируют с нашей целью, чтобы не путать модель лишним шумом." }
            ],
            quiz: {
                type: "code",
                question: "Посчитай корреляцию между столбцами 'price' and 'sales' в датафрейме `df` и сохрани в `corr_val`.",
                setup: "import pandas as pd\ndf = pd.read_csv('games.csv')",
                answer: ["corr_val = df['price'].corr(df['sales'])"],
                hint: "df['col1'].corr(df['col2'])"
            }
        }
    ]
};
export default module6;
