const module2 = {
    id: 2, title: "NumPy", icon: "🔢",
    description: "Быстрые вычисления: от обычных списков до скоростных массивов.",
    lessons: [
        {
            id: "2-1", title: "Знакомство с массивами",
            theory: [
                { type: "text", content: "### 1. Аналогия: Списки vs Массивы\nОбычный список Python — это сумка, в которой лежат разные вещи (яблоко, ключ, книга). Чтобы найти что-то, нужно перерыть всё. \n\n**Массив NumPy** — это лоток для яиц. Всё аккуратно разложено, одного типа и размера. Благодаря этой строгости компьютер обрабатывает массивы в 50-100 раз быстрее." },
                { type: "text", content: "### 2. Создание массива" },
                {
                    type: "code", language: "python", content: `import numpy as np
# Лоток на 5 ячеек
arr = np.array([1, 2, 3, 4, 5])
# Пустая заготовка из нулей
zeros = np.zeros(5)`
                },
                { type: "text", content: "### 3. Практический пример: Shape\nВ DS мы всегда проверяем «форму» данных (`shape`). Это как проверить размер контейнера перед погрузкой:" },
                {
                    type: "code", language: "python", content: `print(arr.shape) # (5,) — 5 элементов в ряд
# Изменение типа для экономии памяти (важно для терабайтов данных!)
arr_small = arr.astype('int8')`
                }
            ],
            quiz: {
                type: "code",
                question: "Импортируй numpy как `np` и создай массив `a` из списка [10, 20, 30].",
                setup: "",
                answer: ["import numpy as np\na = np.array([10, 20, 30])"],
                hint: "np.array([список])"
            }
        },
        {
            id: "2-2", title: "Векторизация: Магия без циклов",
            theory: [
                { type: "text", content: "### 1. Аналогия: Один за всех\nПредставь, что тебе нужно покрасить 100 заборов. \n• **Цикл for:** Ты красишь один, потом идешь к следующему.\n• **Векторизация (NumPy):** Ты нажимаешь кнопку, и армия роботов красит все 100 заборов одновременно.\n\nВ коде это значит, что мы не пишем циклы, а просто умножаем массив на число." },
                { type: "text", content: "### 2. Примеры вычислений" },
                {
                    type: "code", language: "python", content: `prices = np.array([10, 20, 30])
# Роботы умножают каждое число на 1.5 сами:
new_prices = prices * 1.5`
                },
                { type: "text", content: "### 3. Практический пример: Нормализация\nПеред подачей данных в нейросеть их нужно привести к шкале от 0 до 1. С NumPy это делается одной формулой для миллионов строк:" },
                {
                    type: "code", language: "python", content: `data = np.array([10, 50, 100])
normalized = (data - data.min()) / (data.max() - data.min())
# Теперь данные лежат в диапазоне [0, 1]`
                }
            ],
            quiz: {
                type: "code",
                question: "У тебя есть массив `arr = np.array([1, 2, 3, 4, 5])`. Умножь его на 10 и сохрани в `arr_10`.",
                setup: "import numpy as np\narr = np.array([1, 2, 3, 4, 5])",
                answer: ["arr_10 = arr * 10"],
                hint: "Просто используй оператор *"
            }
        },
        {
            id: "2-3", title: "Маскирование (Фильтрация)",
            theory: [
                { type: "text", content: "### 1. Аналогия: Трафарет\n**Маскирование** — это как наложить трафарет на лист бумаги и закрасить только дырки. Мы создаем условие (трафарет), и NumPy оставляет только те данные, что подходят." },
                { type: "text", content: "### 2. Как это работает" },
                {
                    type: "code", language: "python", content: `scores = np.array([8, 4, 9, 2, 7])
mask = scores > 5 # Создает трафарет [True, False, True, False, True]
high_scores = scores[mask] # Оставляет [8, 9, 7]`
                },
                { type: "text", content: "### 3. Практический пример: Очистка аномалий\nЧасто в данных встречаются невозможные значения (например, возраст 200 лет). Мы убираем их маской:" },
                {
                    type: "code", language: "python", content: `# Оставляем только реалистичный возраст
valid_ages = ages[(ages > 0) & (ages < 120)]`
                }
            ],
            quiz: {
                type: "code",
                question: "Из массива `ages = np.array([18, 15, 22, 14, 30])` выбери только те, что >= 18, и сохрани в `adults`.",
                setup: "import numpy as np\nages = np.array([18, 15, 22, 14, 30])",
                answer: ["adults = ages[ages >= 18]"],
                hint: "ages[условие]"
            }
        }
    ]
};
export default module2;
